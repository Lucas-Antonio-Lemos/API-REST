# API-REST
Desenvolvi uma API REST em Node.js para gerenciar produtos de uma loja.  Ela segue o padrão CRUD: POST → adiciona produto GET → lista ou busca por ID PUT → atualiza produto DELETE → remove produto Tudo foi estruturado com Express e um “banco em memória”, ideal para prototipar sem precisar configurar um DB real. 
